{
    "features": {
        "router": {
            "routes": {
                "/:controller?/:action?/:id?.:format?": [ "@controllers", "dispatch" ]
            },

            "baseUrl": "{{url}}",

            "auto": true
        }
    }
}